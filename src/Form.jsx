import React from "react";
import { withRouter } from "react-router";

class Form extends React.Component {
  // UI
  // name
  // address

  // form{} -> add time, generate id

  //submit -> putform()
  render() {
    return <p>Form.</p>;
  }
}

export default withRouter(Form);
